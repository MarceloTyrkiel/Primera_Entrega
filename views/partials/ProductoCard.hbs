<div class="card">
  <div class="card-body">
    <h3>{{title}}</h3>
    <p>{{description}}</p>
    <p>{{code}}</p>
    <p>{{thumbnail}}</p>
    <p>{{price}}</p>
    <p>{{stock}}</p>
  </div>
  <div class="card-footer d-flex justify-content-between align-items-center">
    <a href="/api/producto/delete/{{id}}" class="btn btn-danger">
      Eliminar
    </a>
    <a href="/api/producto/edit/{{id}}" class="btn btn-secondary">
      Editar
    </a>
  </div>
</div>